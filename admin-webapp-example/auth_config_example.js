const config = {
    issuer: "The Okta tenant's default authorization server Issuer URI",
    clientId: "The application Client ID",
    redirectUri: "http://localhost:8080/login/callback"
}

export default config